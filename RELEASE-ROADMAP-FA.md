# نقشه راه نسخه‌های ADB Connect

[English](RELEASE-ROADMAP-EN.md)

مبنا: نسخه 1.6.4؛ ترتیب مطابق درخواست کاربر در ۲۰۲۶-۰۹-۱۳.
این سند جایگزین ترتیب نسخه‌های LAB-UPGRADE-PLAN-FA.md است؛ یافته‌های فنی آن سند معتبر باقی می‌مانند. این تغییر صرفاً برنامه‌ریزی است و قابلیت‌های زیر هنوز پیاده‌سازی نشده‌اند.

## قواعد مشترک

- WinForms و مسیر فعلی محصول حفظ شود.
- Screenshot فعلی از ویندوز حفظ شود. Recorder نیز تصویر نمایش‌داده‌شده در ویندوز را ضبط کند؛ ضبط مستقیم روی Android یا قابلیت record خود scrcpy مبنای این برنامه نیست.
- از اولین مرحله، هر عملیات به Serial ثابت دستگاه متصل شود؛ رابط چنددستگاهی در مرحله تعیین‌شده عرضه شود.
- برای خروجی‌ها از ابتدا شناسه دستگاه، زمان، نوع خروجی و نام فایل یکتا ثبت شود تا افزودن جلسات بعداً نیازمند بازنویسی نباشد.
- نسخه‌ها پیشنهادی هستند؛ زمان قطعی پس از پیاده‌سازی نمونه ضبط و آزمون روی دستگاه و رایانه مرجع تعیین شود.

## 1.6.5 — رفع ایرادهای فعلی

دامنه:
- نتیجه واقعی نصب، حذف، Start و Stop؛ پیام موفقیت جزئی و خطای هر اپ؛ بازخوانی فهرست پس از حذف.
- تفکیک اتصال و مشغول‌بودن، بررسی مجدد پس از Reboot/قطع ارتباط، ثابت‌بودن دستگاه مقصد در کل عملیات.
- مدیریت یکپارچه خطا، timeout، لغو و پایان پردازش‌های متعلق به برنامه؛ حذف یا ادغام AdbProgressRunner بلااستفاده.
- اصلاح دسترسی thread در Logcat، محدودکردن بافر نمایش و ذخیره پیوسته فایل خام مستقل از فیلتر نمایش؛ تفکیک پاک‌کردن نمایش و بافر دستگاه.
- هماهنگی تنظیمات نصب‌کننده، کشف Inno Setup، مستندات امضا و نسخه محصول. بررسی استفاده هماهنگ از ADB توسط برنامه و scrcpy.
- استخراج حداقلی سرویس اجرای فرمان و مدل دستگاه برای توسعه بعدی؛ بدون بازنویسی کامل UI.

معیار تحویل: حذف ناموفق موفق گزارش نشود؛ قطع ارتباط موجب تغییر مقصد نشود؛ ثبت یک ساعت Logcat پرتراکم بدون قفل UI؛ بستن برنامه فرآیندهای خودش را ببندد؛ قابلیت‌های موجود و نصب/ارتقای x64 و x86 آزموده شوند.

## 1.7.0 — Screen Recorder برای یک دستگاه

دامنه:
- Start Recording، Stop Recording، زمان سپری‌شده، وضعیت ضبط، انتخاب مسیر و بازکردن پوشه خروجی.
- هدف اولیه MP4/H.264 با نرخ‌های پیشنهادی 15 و 30 فریم؛ ابعاد خروجی ثابت طی ضبط، حفظ نسبت تصویر و رفتار مشخص هنگام Resize.
- ثبت تصویر هم‌زمان با ضبط؛ کنترل فضای دیسک، خطای encoder و قطع ارتباط؛ نهایی‌کردن فایل هنگام Stop و بستن عادی پنجره.
- در شروع، ضبط تصویر بدون صدا مبنای نسخه است. صدای سیستم/صدای اختصاصی هر دستگاه نیازمندی جداست؛ صدای همه برنامه‌های ویندوز نباید ناخواسته به ویدئو افزوده شود.

بررسی فنی ابتدای مرحله:
1. نمونه کوچک ضبط ناحیه ویدئو از ویندوز و ذخیره فایل قابل پخش.
2. روش فعلی CopyFromScreen محتوای قابل مشاهده مختصات صفحه را می‌گیرد؛ اگر پنجره دیگری روی آن بیاید، آن پنجره هم در ضبط ناحیه وارد می‌شود. Minimize یا قفل سیستم نیز وضعیت نیازمند تشخیص/آزمون است. این محدودیت باید در UI روشن باشد.
3. Windows.Graphics.Capture گزینه ویندوزی برای دریافت فریم پنجره است؛ سازگاری آن با پنجره scrcpy جاسازی‌شده، cropping، نسخه ویندوز و x86 باید در نمونه بررسی شود. جایگزینی Screenshot فعلی جزو این مرحله نیست. انتخاب backend ضبط باید بر اساس نتیجه نمونه ثبت شود؛ پشتیبانی پنجره پوشیده یا Minimize پیشاپیش وعده داده نشود.
4. encoder ویندوز گزینه اولیه بررسی است. وجود DLLهای FFmpeg کنار scrcpy به معنی وجود ابزار مستقل ffmpeg.exe نیست؛ وابستگی جدید باید صریح ارزیابی و بسته‌بندی شود.
5. هنگام قطع دستگاه، خروج scrcpy یا fallback نسخه سازگار، ضبط باید به‌طور مشخص متوقف/بخش‌بندی شود؛ فریم ثابت نباید بی‌اطلاع به عنوان ضبط سالم ادامه یابد.

معیار تحویل: ویدئوی ۳۰ دقیقه‌ای روی رایانه مرجع قابل پخش و از نظر زمان صحیح باشد؛ Screenshot وسط ضبط کار کند؛ تغییر اندازه، پوشیده‌شدن، Minimize، قطع دستگاه و توقف عادی رفتار مشخص داشته باشند. بازیابی پس از crash ناگهانی، تضمین عمومی MP4 نیست و باید جدا آزموده شود.

## 1.8.0 — فایل‌منیجر دستگاه

دامنه:
- مرور پوشه‌های قابل دسترس از طریق ADB، مسیر جاری، بازگشت، Refresh، نام/نوع/حجم/زمان فایل در صورت دسترس‌بودن.
- دانلود به رایانه (Pull)، ارسال به دستگاه (Push)، انتخاب چند فایل و صف انتقال با پیشرفت واقعی در حد داده موجود، لغو و نتیجه هر فایل.
- ساخت پوشه، تغییرنام و حذف با نمایش دقیق مسیر و دستگاه مقصد و تأیید عملیات حذف؛ سیاست صریح برای فایل هم‌نام.
- پشتیبانی نام فارسی، فاصله و نویسه‌های خاص؛ جداسازی مسیر Windows و Android، اعتبارسنجی و escaping دستورهای shell.
- انتقال ابتدا به فایل موقت و جایگزینی نهایی پس از موفقیت هرجا پشتیبانی شود؛ نمایش فایل ناقص در صورت شکست؛ عدم ادعای resume در نسخه اول.
- نمایش Permission denied به‌صورت روشن؛ بدون فرض root یا دسترسی به همه پوشه‌های سیستم؛ symlinkها برای جلوگیری از پیمایش اشتباه مشخص شوند.

معیار تحویل: انتقال فایل کوچک و بزرگ با مقایسه hash در صورت امکان، نام فارسی/فاصله، تعارض نام، فضای ناکافی و قطع انتقال آزموده شود؛ شکست انتقال موفق اعلام نشود و فایل قبلی بی‌اطلاع جایگزین نشود. ضبط و Logcat حین انتقال نیز آزموده شوند.

## 1.9.0 — Screenshot و Recorder چنددستگاهی

دامنه:
- مدیریت مستقل DeviceContext شامل Serial، ScrcpyRunner، پنجره، Recorder، وضعیت و مسیر خروجی برای هر دستگاه.
- بازشدن چند پنجره کنترل؛ هر پنجره Screenshot و Start/Stop Recording مستقل داشته باشد.
- Screenshot گروهی و Start/Stop گروهی روی دستگاه‌های انتخاب‌شده، با زمان و نتیجه مستقل هر دستگاه.
- هم‌زمانی به معنی شروع نزدیک به هم است، نه هم‌زمانی دقیق فریم؛ اختلاف زمان شروع ثبت شود.
- فایل‌منیجر همواره دستگاه مقصد واضح داشته باشد؛ انتقال مستقیم بین دو دستگاه در دامنه این نسخه نیست.
- صف فرمان‌های تغییر‌دهنده به‌ازای هر دستگاه؛ جریان‌های لاگ/ضبط مستقل و سقف هم‌زمانی متناسب با توان رایانه.
- آزمون اولیه دو دستگاه، سپس چهار دستگاه روی سخت‌افزار مرجع؛ سقف پشتیبانی بر اساس اندازه‌گیری CPU/GPU/RAM/دیسک تعیین شود.
- اگر backend ناحیه صفحه استفاده شود، پنجره‌های تحت ضبط باید قابل مشاهده و بدون هم‌پوشانی چیده شوند. برداشت خودکار از پنجره‌های پوشیده تنها پس از تأیید backend پنجره‌ای ارائه شود. Screenshot موجود نیز این محدودیت را دارد؛ گروهی‌بودن به‌تنهایی آن را رفع نمی‌کند.

معیار تحویل: دو ضبط مستقل ۳۰ دقیقه‌ای و Screenshot هنگام ضبط؛ توقف یا قطع یک دستگاه روی دیگری اثر نگذارد؛ شواهد به دستگاه اشتباه منتسب نشوند؛ resize/جابجایی/افت فریم و برخورد منابع مدیریت و گزارش شوند.

## 2.0.0 — مدیریت جلسات آزمایش

دامنه: ایجاد جلسه با نام، دستگاه/دستگاه‌ها، فریمور، اپ تحت تست، توضیحات و نتیجه دستی؛ اتصال لاگ، Screenshot، ویدئو و Bugreport به جلسه؛ تاریخچه، جست‌وجو و گزارش HTML/ZIP. انتقال فایل لازم نیست خودکار به شواهد جلسه اضافه شود؛ پیوست انتخابی باشد. محل ذخیره داده قابل تنظیم و مستقل از پوشه نصب باشد.

معیار تحویل: جلسه پس از بازکردن دوباره برنامه با تمام ارجاع‌های فایل قابل بازیابی باشد؛ شواهد دو جلسه مخلوط نشوند؛ داده‌های قبلی هنگام ارتقا حفظ شوند.

## 2.1.0 — سناریوها و تست‌های تکرارشونده

دامنه: مراحل نسخه‌دار با پیش‌شرط، انتظار، timeout، لغو، تعداد تکرار و شواهد هنگام شکست؛ سناریوی Reboot و Launch/Stop اپ؛ نتایج Passed/Failed/Error/Skipped/Canceled؛ اجرای گروهی بر بستر 1.9 و ذخیره در جلسات 2.0. تست‌های تخصصی صدا/تصویر و تجهیزات خارجی پس از تعیین روش اندازه‌گیری تعریف شوند.

معیار تحویل: اجرای ۲۰ چرخه روی دستگاه مرجع با نتیجه هر چرخه؛ لغو نتیجه نیمه‌تمام را حفظ کند؛ عملیات تغییر‌دهنده نامطمئن پس از crash خودکار تکرار نشود.

## روتین ثابت هر نسخه

1. تثبیت دامنه و معیار پذیرش در همین سند؛ نگهداری نسخه مبنای قابل بازگشت و ثبت تغییرات سورس.
2. نمونه فنی فقط برای بخش‌های نامطمئن، سپس پیاده‌سازی کوچک و قابل بررسی.
3. Build و آزمون خودکار هدفمند برای منطق حساس؛ تست روی دستگاه واقعی.
4. بررسی عدم خرابی قابلیت‌های نسخه قبلی، به‌ویژه Pairing، Logcat، Screenshot و fallback در scrcpy.
5. ساخت نسخه آزمایشی و استفاده در آزمایشگاه؛ ثبت مشکلات و رفع موارد مانع انتشار.
6. خروجی نهایی x64/x86، تست نصب و ارتقا، یادداشت تغییرات و انتشار پس از تکمیل پذیرش نسخه.

## منابع فنی ضبط ویندوز

- https://learn.microsoft.com/en-us/windows/uwp/audio-video-camera/screen-capture
- https://learn.microsoft.com/en-us/windows/uwp/audio-video-camera/screen-capture-video
- https://github.com/MicrosoftDocs/SimpleRecorder
