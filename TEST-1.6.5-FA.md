# راهنمای تست ADB Connect 1.6.5

[English](TEST-1.6.5-EN.md)

بستهٔ نهایی 1.6.5 هر دو اصلاحیه را دارد؛ Cancel operation فقط هنگام تولید واقعی Bugreport ظاهر می‌شود.

این نسخه برای بررسی روی دستگاه آزمایشگاهی آماده می‌شود. آزمون‌های خودکار جایگزین تست دستگاه واقعی نیستند. Screenshot همچنان از ناحیه قابل مشاهده ویندوز گرفته می‌شود؛ Recorder و فایل‌منیجر جزو این نسخه نیستند.

## اجرای نسخه

خروجی‌های قابل حمل در `publish/v1.6.5/x64` و `publish/v1.6.5/x86` هستند. فایل `ADB Connect.exe` را از داخل همان پوشه اجرا کنید؛ زیرپوشه‌های platform-tools و scrcpy باید کنار آن باقی بمانند. برای شروع روی Windows 64-bit از x64 استفاده کنید. خروجی محلی امضای دیجیتال ندارد.

## بررسی روی دستگاه

1. اتصال و مشخصات: اتصال IP یا Pairing را برقرار کنید، دستگاه انتخابی و نسخه Android را بررسی کنید. در بخش TV Control، Refresh دستگاه‌های ADB از جمله USB را نشان می‌دهد.
2. اپ آزمایشی: APK بی‌خطر و قابل حذف را نصب کنید، فهرست Apps را بخوانید، آن را اجرا و متوقف کنید. پیام هر اپ باید با نتیجه واقعی یکسان باشد.
3. حذف: فقط اپ آزمایشی را انتخاب و حذف کنید. این نسخه همچنان حذف برای user 0 انجام می‌دهد. فهرست پس از عملیات از دستگاه دوباره خوانده می‌شود. از اپ‌های سیستمی برای تست شکست استفاده نکنید؛ شکست مصنوعی در تست‌های خودکار پوشش داده شده است.
4. Logcat: Start Log، اعمال Keyword و محدودکردن نمایش، سپس Export Log. فایل خروجی باید از فیلتر کلمه و حذف خطوط قدیمی نمایش مستقل باشد. اگر Package تعیین شود، خود ADB جریان را به PID فعلی آن اپ محدود می‌کند؛ پس از Restart اپ برای PID جدید ثبت لاگ را دوباره شروع کنید.
5. Clear View فقط صفحه را پاک می‌کند. دکمه جداگانه Clear device log buffer پس از تأیید، بافر Android را پاک می‌کند؛ فایل‌های ذخیره‌شده باقی می‌مانند.
6. Stop Log و شروع مجدد: هر بار فایل جدید ساخته شود و پیام خطای کاذب برای توقف عمدی نشان داده نشود.
7. لغو: روی عملیات طولانی مثل Bugreport، Cancel operation را بزنید. لغو به معنی برگرداندن تغییرات انجام‌شده روی دستگاه نیست.
8. قطع ارتباط: اتصال دستگاه آزمایشی را قطع و یک فرمان/Refresh اجرا کنید؛ برنامه نباید دستگاه دیگری را به‌عنوان جایگزین هدف انتخاب کند. پس از وصل‌شدن همان دستگاه Refresh کنید.
9. در زمان مناسب و بدون کار ذخیره‌نشده، Reboot معمولی را بررسی کنید. پیام باید «درخواست Reboot» باشد؛ پس از Boot با Refresh اتصال را بررسی کنید. Recovery/Bootloader/Fastboot برای این چک اولیه لازم نیستند.
10. TV Control و Screenshot را با Logcat هم‌زمان بررسی کنید، سپس برنامه را هنگام ثبت لاگ ببندید. پردازش ثبت لاگ متعلق به این اجرا باید پایان یابد؛ سرویس مشترک ADB عمداً kill-server نمی‌شود.

## محل لاگ و گزارش ایراد

فایل‌های خام در `%LOCALAPPDATA%\ADB Connect\Logs` نگهداری می‌شوند و مسیر فایل در شروع لاگ نمایش داده می‌شود. فایل‌های قدیمی خودکار پاک نمی‌شوند؛ فضای دیسک را برای تست‌های طولانی در نظر بگیرید. در صورت کندی نمایش ممکن است خطوط نمایش کنار گذاشته شوند؛ ذخیره فایل خام مستقل است. اگر نوشتن فایل شکست بخورد، ضبط متوقف و خطا گزارش می‌شود.

برای هر ایراد این موارد را ثبت کنید: معماری برنامه، مدل و نسخه Android، نوع اتصال، مراحل تکرار، نتیجه مورد انتظار، متن خطا و در صورت نیاز تصویر. برای ارسال لاگ، ابتدا محتوای شخصی آن را بررسی کنید.

## بررسی‌های توسعه

- `dotnet run --project tests/RegressionTests.csproj`: سنجش نتیجه اپ، آرگومان فارسی، خروجی حجیم stdout/stderr، timeout، لغو، پایان پردازش، فایل لاگ مستقل از بافر نمایش و توقف طبیعی/عمدی؛ بدون اتصال به Android.
- Build و Publish x64/x86 و ساخت نصب‌کننده، صحت بسته‌بندی را بررسی می‌کنند؛ نصب روی Windows تمیز، تست واقعی x86 و تست طولانی سخت‌افزار هنوز باید انجام شوند.
- آزمون پیشنهادی پایداری: یک ساعت Logcat واقعی همراه با استفاده از UI و Screenshot.
