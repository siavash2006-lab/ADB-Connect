# پیش‌نمایش سوم: چیدمان فشرده و انتخاب تم

[English](UI-PREVIEW-EN.md)

اندازهٔ پنجره از ۷۲۰×۷۹۰ به ۶۲۰×۶۴۶ پیکسل منطقی کاهش یافت. اتصال، کنترل‌های ریبوت، مشخصات، عملیات برنامه‌ها و فیلترهای لاگ در ردیف‌های منظم با فاصلهٔ کمتر قرار دارند.

سه دکمهٔ آیکونی مانیتور (سیستم، پیش‌فرض)، خورشید (روز) و ماه (شب) با راهنمای شناور و نشانگر انتخاب اضافه شدند. تب‌ها و فهرست دستگاه زمینهٔ ملایم و حاشیهٔ ظریف دارند و قاب سفید برجسته حذف شده است. حالت System از تنظیم رنگ برنامه‌ها در ویندوز پیروی می‌کند و به تغییر آن پاسخ می‌دهد. انتخاب کاربر در %LOCALAPPDATA%/ADB Connect/theme.txt ذخیره می‌شود. پنجرهٔ Pair، نوار کنترل تلویزیون، مشخصات و نتیجه‌ها و تأییدها از تم مشترک استفاده می‌کنند. پنجره‌های انتخاب فایل ویندوز ظاهر مدیریت‌شده توسط سیستم‌عامل را دارند.

فایل publish/v1.6.5-ui-preview3/x64/ADB Connect.exe را همراه پوشه‌های کنارش اجرا کن. اصلاحیهٔ fix3 در آن وجود دارد؛ بسته‌های منتشرشده تغییری نکرده‌اند.

بررسی‌ها: هر دو تم در چهار تب، Pair، نتیجه‌ها، تأییدها و نوار کنترل تلویزیون بدون اجرای scrcpy رندر شدند. تغییر زندهٔ تم میان پنجره‌ها و پیش‌فرض No در تأیید عملیات مخرب بررسی شد. چیدمان با مقیاس‌دهی برنامه‌ای ۱۵۰٪ نیز رندر شد؛ این جای تست واقعی DPI چند نمایشگر را نمی‌گیرد. هر ۲۳ بررسی قبلی پاس شدند. عملیات روی دستگاه واقعی اجرا نشده است.


## پیش‌نمایش چهارم: نمایش Properties داخل تب
نتیجه، وضعیت خواندن و خطای هر ۲۷ گزینه داخل تب Properties نمایش داده می‌شود. نام دستگاه کنار نتیجه ثبت می‌شود؛ انتخاب متن، Copy، Save و اسکرول برای مقادیر طولانی وجود دارد. Read All Properties ابتدا اطلاعات را نمایش می‌دهد و ذخیره‌سازی اختیاری است. خروجی جدید: publish/v1.6.5-ui-preview4/x64/ADB Connect.exe. مقادیر نمونهٔ کوتاه و بلند در هر دو تم بررسی شدند؛ خواندن از دستگاه واقعی هنوز نیاز به بررسی دارد.


## پیش‌نمایش پنجم: کلید تکی تم
یک دکمهٔ خورشید/ماه، تم فعلی را نشان می‌دهد و با کلیک به حالت مقابل می‌رود. در اولین استفاده، تم ویندوز اعمال می‌شود؛ پس از تغییر دستی، انتخاب ذخیره می‌شود. آیکون کامپیوتر حذف شد. خروجی: publish/v1.6.5-ui-preview5/x64/ADB Connect.exe.
