# برنامه ارتقای ADB Connect برای آزمایشگاه

[English](LAB-UPGRADE-PLAN-EN.md)

> ترتیب نسخه‌های این سند با درخواست جدید کاربر جایگزین شده است. مرجع جاری برنامه اجرا، `RELEASE-ROADMAP-FA.md` است: رفع ایرادها → ضبط از ویندوز → فایل‌منیجر → تصویربرداری چنددستگاهی → جلسات و تست‌ها. یافته‌های بررسی کد در این سند همچنان مبنای اصلاحات هستند.

تاریخ بررسی: ۲۰۲۶-۰۹-۱۳ — مبنا: سورس محلی نسخه 1.6.4

## مبنا و حدود بررسی

- پروژه C# / Windows Forms با هدف `net10.0-windows` و خروجی‌های win-x64 و win-x86 است.
- گفت‌وگوی اشتراکی قابل مشاهده بررسی شد: تمرکز آن بر انتشار EXE، ساخت نصب‌کننده، حفظ زیرپوشه platform-tools و خطای robocopy در انتشار x86 است. تصاویر صرفاً با عنوان Uploaded an image ظاهر شدند؛ محتوای تصاویر مبنای نتیجه‌گیری نیست.
- فایل‌های اصلی فرم، اجرای ADB، Pairing، scrcpy، ثبت تصویر، تنظیمات پروژه و اسکریپت‌های انتشار بررسی شدند.
- `dotnet build "ADB Connect.csproj" --no-restore -v:minimal` موفق شد: صفر خطا و صفر هشدار. تلاش اولیه خطای دسترسی به خروجی resources داشت؛ اجرای مجاز مجدد موفق بود. این نتیجه Build افزایشی Debug است، نه تأیید انتشار تمیز x86/x64.
- برنامه روی دستگاه واقعی اجرا نشده و نصب‌کننده نصب یا تولید نشده است. پروژه تست خودکار در فایل‌های بررسی‌شده پیدا نشد.
- پوشه فعلی مخزن Git نیست؛ README آدرس مخزن عمومی را معرفی می‌کند، ولی همسانی سورس محلی با آن تأیید نشده است.
- فرض برنامه: آزمایشگاه Android TV/Android برای بررسی فریمور، اپ و عیب‌یابی. تعداد دستگاه‌ها، نوع بردها و تست‌های اولویت‌دار هنوز مشخص نشده‌اند.

## قابلیت‌های موجود

اتصال شبکه و Pairing با پورت‌های مستقل، شناسایی دستگاه‌های ADB، انتخاب دستگاه فعال، دریافت مشخصات سخت‌افزار و نرم‌افزار، نصب APK، فهرست و اجرای/توقف/حذف اپ‌ها، Logcat با فیلتر، Bugreport، خروجی getprop، کنترل تصویر و صدا با scrcpy و ثبت تصویر پنجره.

در scrcpy سازوکار بازگشت به نسخه سازگار در صورت افت شدید ابعاد تصویر وجود دارد. این رفتار باید در بازآرایی حفظ و روی مدل‌های واقعی دوباره بررسی شود. نام دکمه Fastboot در نسخه فعلی به معنی اجرای `adb reboot fastboot` است؛ ماژول مدیریت یا فلش Fastboot وجود ندارد.

## یافته‌های اصلی و ترتیب اصلاح

| اولویت | شاهد در سورس | اثر در آزمایشگاه | تغییر پیشنهادی |
|---|---|---|---|
| P0 | Form1.cs: متغیر `_isConnected` برای غیرفعال‌کردن UI حین عملیات هم استفاده می‌شود؛ چند مسیر آن را بدون بازبینی دستگاه true می‌کنند | نمایش اتصال پس از Reboot/قطع شبکه؛ وضعیت اشتباه پس از خطا | جداکردن وضعیت اتصال و عملیات؛ تأیید مجدد آمادگی دستگاه |
| P0 | Form1.cs: `btnUninstall_Click_1` پس از شکست هم آیتم‌ها را حذف و پیام موفقیت نمایش می‌دهد؛ Start/Stop نیز پیام نهایی مستقل از نتیجه دارند | نتیجه آزمایش نامعتبر | ثبت نتیجه واقعی هر اپ، موفقیت جزئی و خطا؛ بازخوانی فهرست از دستگاه |
| P0 | Form1.cs: دستگاه فعال متغیر سراسری است؛ حلقه عملیات چند اپ در هر تکرار از آن استفاده می‌کند | امکان تغییر مقصد بین مراحل با مسیرهای اتصال/انتخاب | ثبت Serial ثابت در ابتدای هر عملیات؛ عدم انتقال خودکار کار به دستگاه دیگر |
| P0 | Form1.cs: Logcat فیلتر UI را از callback پس‌زمینه می‌خواند و هر خط را با Invoke به UI می‌فرستد؛ صف تعریف شده به این مسیر متصل نیست | ریسک دسترسی بین threadها و کندی لاگ پرتراکم | snapshot فیلترها، صف محدود برای نمایش و نوشتن مستقل روی دیسک |
| P0 | Form1.cs: Export Log متن فعلی RichTextBox را ذخیره می‌کند؛ نمایش خطوط قدیمی را حذف می‌کند | از دست رفتن شواهد تست طولانی | فایل خام پیوسته با چرخش فایل؛ فیلتر فقط روی نمایش/خروجی انتخابی |
| P0 | Form1.cs: پایان فرم فقط scrcpy را Dispose می‌کند؛ Logcat پاک‌سازی صریح ندارد | احتمال باقی‌ماندن فرآیند و callback پس از بسته‌شدن فرم | لغو عملیات و بستن/Dispose فرآیندهای متعلق به برنامه |
| P1 | AdbRunner.cs: قفل سراسری برای اجرای دستورها و Bugreport | گزارش طولانی کارهای کوتاه و توسعه چنددستگاهی را معطل می‌کند؛ Logcat فعلی مسیر جدا دارد | صف عملیات تغییر‌دهنده برای هر دستگاه؛ جریان‌های مستقل لاگ و محدودیت هم‌زمانی |
| P1 | Form1.cs: `packageListUpdate` از نوع async void است و مسیر خطا وضعیت را برنمی‌گرداند | مدیریت دشوار خطا و UI غیرفعال | Task، try/finally و وضعیت عملیات مستقل |
| P1 | AdbProgressRunner.cs: نتیجه WaitForExit(timeoutMs) نادیده گرفته می‌شود؛ exit code و لغو ندارد؛ نمونه آن در فرم استفاده مؤثر ندارد | مسیر اجرای موازی و ناقص | حذف مسیر بلااستفاده یا ادغام با اجرای استاندارد |
| P1 | ScrcpyHostForm.cs: استفاده از CopyFromScreen؛ AdbRunner.cs متد CaptureScreenshotAsync جداگانه دارد | تصویر به اندازه و پوشیده‌شدن پنجره وابسته است | دو انتخاب واضح «تصویر دستگاه» و «تصویر پنجره»، با ثبت روش و ابعاد |
| P1 | Build-Installers.ps1: کشف خودکار فقط Inno Setup 6؛ اسکریپت‌های iss فاقد SignTool و دارای SignedUninstaller=no هستند | مستندات و پیام «Signed» اسکریپت تضمین امضای خروجی نیست | تشخیص نسخه نصب‌شده، هماهنگ‌کردن مستندات و بررسی امضای خروجی |
| P1 | Form1.cs حدود ۱۸۰۰ خط و شامل UI، منطق دستگاه و عملیات است | افزودن هر قابلیت ریسک تغییر قابلیت‌های قبلی را زیاد می‌کند | استخراج تدریجی سرویس‌ها و مدل‌ها با حفظ WinForms |

## محصول هدف

واحد اصلی کار یک «جلسه آزمایش» باشد: دستگاه مشخص + نسخه فریمور + اپ تحت تست + سناریو + نتایج + شواهد. هر جلسه شناسه مستقل، نام اپراتور اختیاری، زمان شروع/پایان، توضیحات و وضعیت نهایی داشته باشد.

صفحات پیشنهادی: دستگاه‌ها، نمای کلی دستگاه، اپ‌ها، لاگ و عیب‌یابی، کنترل تلویزیون، سناریوها و گزارش‌ها. انتخاب دستگاه در نوار مشترک همه صفحات دیده شود. Device Properties به جدول قابل جست‌وجو با کپی، ذخیره و مقایسه تبدیل شود؛ پیام‌های اطلاعاتی پرتکرار داخل صفحه نمایش داده شوند.

## مرحله ۱ — تثبیت پایه (نسخه پیشنهادی 1.7)

1. تعیین نسخه مبنا و ثبت تغییرات با Git پس از بررسی رابطه سورس محلی و مخزن معرفی‌شده.
2. جداسازی ConnectionState از OperationState؛ ثبت Serial ثابت در هر کار.
3. اجرای مشترک دستورها با آرگومان‌های ساختاریافته، timeout، cancellation و خروجی استاندارد شامل exit code/stdout/stderr/duration/status. آرگومان‌های Android shell نیز اعتبارسنجی و نقل‌قول مناسب لازم دارند.
4. اصلاح نتیجه نصب/حذف/اجرا/توقف، جلوگیری از اجرای تکراری و اعلام موفقیت جزئی.
5. اصلاح مسیر Logcat، ثبت کامل فایل، مدیریت پایان فرآیند و تفکیک «پاک‌کردن نمایش» از پاک‌کردن بافر دستگاه.
6. کنترل وابستگی‌ها و نسخه‌ها در شروع برنامه؛ استفاده هماهنگ ADB در برنامه و scrcpy.
7. هماهنگ‌کردن نصب‌کننده، نسخه محصول و مستندات انتشار؛ حفظ ساختار زیرپوشه‌های موجود در publish.

پذیرش: خطای عمدی اپ موفق گزارش نشود؛ قطع دستگاه وسط عملیات مقصد را تغییر ندهد؛ یک ساعت لاگ پرتراکم UI را قفل نکند و فایل خام کامل باشد؛ بستن برنامه فرآیندهای متعلق به خودش را باقی نگذارد. نصب تمیز و ارتقای x64/x86 در محیط‌های هدف با تمام وابستگی‌ها آزموده شود.

## مرحله ۲ — ابزار روزانه آزمایشگاه (نسخه پیشنهادی 1.8)

- فهرست دستگاه‌های ذخیره‌شده: نام آزمایشگاهی، مدل/برد، شناسه پایدار، endpoint فعلی، نوع اتصال و آخرین وضعیت. IP به‌تنهایی هویت دستگاه نباشد.
- نمای کلی دستگاه از getprop و اطلاعات پشتیبانی‌شده: نسخه Android/SDK، Build و Vendor Fingerprint، CPU/ABI، RAM/Storage و نمایشگر. موارد ناموجود با Unsupported/Unavailable مشخص شوند.
- ایجاد جلسه و جمع‌آوری یک‌جای مشخصات، فهرست/نسخه اپ‌ها، Logcat، Bugreport، تصویر و یادداشت.
- مدیریت اپ: نمایش versionName/versionCode، نصب مجدد و نصب دسته‌ای با نتیجه مستقل؛ پاک‌کردن داده با نمایش روشن اثر آن. پشتیبانی split APK در صورت استفاده واقعی آزمایشگاه.
- کنترل تلویزیون: کلیدهای جهت/OK/Home/Back/Volume، ارسال متن، تصویر دستگاه/پنجره، ضبط ویدئو و ذخیره مستقیم در جلسه.
- قالب گزارش HTML قابل چاپ، CSV برای مقایسه جدولی، JSON برای داده اصلی و ZIP شواهد. زمان، Serial، فریمور و نسخه ابزارها در خروجی درج شوند.

پذیرش: برای یک دستگاه، یک جلسه با مشخصات و شواهد بدون جابه‌جایی دستی فایل‌ها تولید شود و پس از بازکردن دوباره برنامه قابل مشاهده باشد. اتصال مجدد و تغییر IP با تأیید هویت دستگاه مدیریت شود.

## مرحله ۳ — تست‌های تکرارشونده (نسخه پیشنهادی 1.9)

موتور سناریو با مراحل مشخص و نسخه‌دار: پیش‌شرط، عملیات، زمان انتظار، شرط موفقیت، timeout، رفتار در شکست، تعداد تکرار و جمع‌آوری شواهد. retry خودکار فقط برای کارهایی باشد که تکرارشان اثر نامطلوب ندارد.

سناریوهای اولیه پیشنهادی، وابسته به اولویت آزمایشگاه:

1. بررسی اولیه فریمور: مشخصات → کنترل مقادیر مورد انتظار → راه‌اندازی اپ → تصویر و گزارش.
2. چرخه Reboot: ثبت زمان → Reboot → انتظار برای بازگشت همان دستگاه و تکمیل Boot → بررسی اپ/Launcher → ثبت نتیجه هر چرخه.
3. پایداری اپ: Launch/Stop تکراری → ثبت Crash/ANR و مصرف منابع قابل دسترس → ذخیره شواهد هنگام شکست.
4. مقایسه قبل و بعد ارتقای فریمور: تفاوت مشخصات، نسخه اپ‌ها و نتایج سناریوی یکسان؛ نصب فریمور در این مرحله پیش‌فرض نیست.
5. شبکه: ثبت وضعیت و آزمون به مقصد مشخص؛ تمایز قطع مسیر ADB از شکست عملکرد دستگاه.

وضعیت‌ها: Passed، Failed، Error، Skipped و Canceled. نبود قابلیت یا نبود دسترسی نباید Pass محسوب شود. تست دستی هم باید قابل ثبت باشد.

پذیرش: سناریوی ۲۰ چرخه‌ای روی دستگاه مرجع با نتیجه و شواهد هر چرخه؛ لغو میان اجرا نتیجه نیمه‌تمام را حفظ کند؛ ازسرگیری پس از crash برنامه، مرحله تغییر‌دهنده نامطمئن را خودکار تکرار نکند.

## مرحله ۴ — کار هم‌زمان و تست‌های تخصصی (نسخه پیشنهادی 2.0)

- اجرای سناریو روی گروه منتخب دستگاه‌ها با محدودیت هم‌زمانی و فایل/نتیجه مستقل هر دستگاه.
- داشبورد مقایسه مدل‌ها و فریمورها، زمان Boot، شکست سناریو و روند داده‌های قابل دسترس.
- پروفایل قابلیت برای مدل/چیپست: دستورهای مشترک و افزونه‌های Vendor از هم جدا شوند.
- تست‌های صدا و تصویر، HDMI/CEC، Bluetooth، Wi-Fi و پخش طولانی فقط پس از تعیین روش اندازه‌گیری و دسترسی واقعی دستگاه طراحی شوند. بخشی به اپ تست روی تلویزیون، API سازنده یا تجهیزات خارجی نیاز دارد؛ ADB به‌تنهایی صحت فیزیکی تصویر/صدا را ثابت نمی‌کند.
- فلش فریمور، Fastboot کامل، کنترل برق و تجهیزات اندازه‌گیری هرکدام ماژول جدا با نیازمندی و معیار پذیرش خود باشند.

پذیرش: سناریوی یکسان روی حداقل دو دستگاه آزمایشی هم‌زمان اجرا شود؛ قطع یکی اجرای دیگری را متوقف نکند؛ هیچ فرمان یا شاهدی به دستگاه دیگر منتسب نشود. مقیاس نهایی پس از اعلام تعداد واقعی دستگاه‌ها تعیین شود.

## ساختار فنی پیشنهادی

WinForms فعلی حفظ شود و استخراج تدریجی انجام گیرد:

- Domain: Device، DeviceCapabilities، LabSession، TestPlan، TestStep، TestResult، Artifact.
- Services: DeviceService، AppService، LogcatService، DiagnosticsService، ScreenService، TestRunner، ReportService.
- Infrastructure: اجرای Process/ADB/scrcpy، ذخیره فایل‌ها و داده‌های محلی.
- UI: فرم‌ها و کنترل‌هایی که سرویس‌ها را صدا می‌زنند؛ منطق نتیجه‌گیری آزمایش در event handler نباشد.

گزینه پیشنهادی برای داده محلی SQLite است؛ فایل‌های حجیم لاگ/ویدئو روی دیسک و مسیر/metadata در پایگاه داده نگهداری شوند. انتخاب کتابخانه و نسخه آن در زمان پیاده‌سازی بررسی شود. مسیر پیش‌فرض داده‌ها یک پوشه قابل نوشتن کاربر باشد، با امکان انتخاب محل پروژه آزمایشگاهی؛ داده‌های جلسه داخل Program Files ذخیره نشوند.

## آزمون و تحویل

- تست واحد برای parser خروجی ADB، وضعیت اتصال، طبقه‌بندی نتایج و قواعد سناریو؛ نمونه خروجی از مدل‌ها و نسخه‌های واقعی لازم است.
- تست یکپارچه با اجرای کنترل‌شده فرایند جعلی برای timeout، قطع ناگهانی، خروجی حجیم، لغو و پایان فرآیند.
- تست سخت‌افزاری برای USB/TCP/Pairing، دو دستگاه، قطع شبکه، Reboot، Logcat+scrcpy و مدل نیازمند fallback.
- تست انتشار روی ماشین‌های هدف: ساختار platform-tools/scrcpy، معماری همه وابستگی‌ها، نصب بدون SDK، حفظ داده در ارتقا و صداقت پیام امضا.
- هر مرحله خروجی قابل استفاده، تغییرات مستند و معیار پذیرش پاس‌شده داشته باشد.

## تصمیم‌های لازم برای تنظیم اولویت

سه کار پرتکرار، مدل/چیپست و نسخه Android دستگاه‌ها، تعداد هم‌زمان، نوع اتصال، وجود root یا userdebug، نمونه گزارش مطلوب و تجهیزات خارجی آزمایشگاه مشخص شوند. تا آن زمان مرحله ۱ و سپس جلسه آزمایش + گزارش + Logcat کامل، اولویت پیشنهادی هستند. زمان‌بندی قطعی پیش از این اطلاعات و آزمون روی دستگاه مرجع قابل اتکا نیست.

## منابع

- گفت‌وگوی قبلی: https://chatgpt.com/share/6aa67450-b05c-83eb-a360-7daf0c82b178
- ADB: https://developer.android.com/tools/adb
- Logcat: https://developer.android.com/tools/logcat
- scrcpy: https://github.com/Genymobile/scrcpy

این سند پیشنهاد توسعه است؛ هیچ‌یک از قابلیت‌های پیشنهادی به معنی پیاده‌سازی یا تأیید سخت‌افزاری فعلی نیست.
