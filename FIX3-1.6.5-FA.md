# پیش‌نمایش اصلاحیهٔ سوم 1.6.5: نمایش برنامه پس از حذف

[English](FIX3-1.6.5-EN.md)

فرمان حذف برای کاربر ۰ اندروید اجرا می‌شود، اما فهرست قبلی کاربر مشخصی نداشت و در برخی نسخه‌های اندروید برنامه‌های کاربران دیگر را نیز نشان می‌دهد. به همین دلیل برنامهٔ حذف‌شده می‌توانست دوباره در فهرست دیده شود و حذف دوباره خطای `not installed for 0` بدهد. انتخاب‌های چندتایی به‌ترتیب پردازش می‌شوند، نه همزمان.

اکنون فهرست‌های All، System و Third-party فقط برنامه‌های نصب‌شده برای کاربر ۰ را بدون گزینهٔ `-u` دریافت می‌کنند. فهرست ذخیره‌شده قبل از تازه‌سازی پاک می‌شود تا شکست تازه‌سازی باعث بازگشت نتایج قدیمی هنگام جست‌وجو نشود. هر حذف موفق با فهرست بدون فیلتر کاربر ۰ کنترل می‌شود و نتیجه نیز محدودهٔ حذف را مشخص می‌کند. این تغییر برنامه را از کاربران دیگر یا پارتیشن سیستمی پاک نمی‌کند.

پیش‌نمایش در `publish/v1.6.5-fix3/x64` قرار دارد. بسته‌های منتشرشدهٔ 1.6.5 تغییر نکرده‌اند. هنگام توسعه هیچ فرمان حذفی روی دستگاه واقعی اجرا نشده است.

## بررسی روی دستگاه

۱. پیش‌نمایش را اجرا و فهرست Apps Management را تازه‌سازی کن؛ برنامه‌های حذف‌شده برای کاربر ۰ نباید نمایش داده شوند.

۲. دو برنامهٔ آزمایشی قابل‌حذف برای کاربر ۰ نصب کن، هر دو را انتخاب و حذف کن.

۳. نتیجهٔ هر دو باید `Uninstalled for user 0 (verified)` باشد.

۴. با تازه‌سازی، تغییر فیلتر و جست‌وجو، برنامه‌های حذف‌شده نباید برگردند.

۵. در صورت شکست یک حذف، نام و خطای همان برنامه باید مشخص باشد و برنامه‌های موفق همچنان از فهرست حذف بمانند.

حذف برنامه، داده‌های کاربر آن را نیز پاک می‌کند؛ در صورت نیاز از برنامه‌های آزمایشی پشتیبان داشته باش. اگر مشکل ادامه داشت، فیلتر انتخاب‌شده، متن نتیجه و خروجی `adb -s SERIAL shell pm list packages --user 0` برای تشخیص لازم است.
