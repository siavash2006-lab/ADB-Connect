# اصلاحیه UI و نمایش لاگ برای 1.6.5

[English](FIX1-1.6.5-EN.md)

یادداشت تاریخی: بستهٔ نهایی 1.6.5 اکنون این اصلاحیه و fix2 را شامل می‌شود.

خروجی آزمایشی جدید: `publish/v1.6.5-fix1/x64` و `publish/v1.6.5-fix1/x86`.
نصب‌کننده‌ها و پوشه `publish/v1.6.5` مربوط به Build قبلی هستند؛ برای این بررسی از خروجی قابل‌حمل fix1 استفاده کنید. اطلاعات نسخه فایل جدید شامل `1.6.5-fix1` است.

- نوار اضافی پایین فرم حذف شد؛ اندازه اصلی فرم حفظ شده است.
- Clear Device... داخل تب Log، کنار سایر عملیات لاگ قرار دارد و همچنان تأیید می‌خواهد.
- Cancel operation فقط هنگام انجام عملیات در فضای میانی پایین قاب اصلی دیده می‌شود.
- تغییر متن/رنگ و حذف خطوط RichTextBox در یک بخش همگام انجام می‌شود که موقتاً حالت ReadOnly را برمی‌دارد و در finally برمی‌گرداند؛ ورودی کاربر در این بخش پردازش نمی‌شود. این اصلاح مسیر احتمالی صدای اخطار RichEdit را برطرف می‌کند، نه صدای کل ویندوز را.
- پیام‌های scrcpy به صف محدود نمایش می‌روند؛ دیگر هر پیام یک BeginInvoke جداگانه برای تغییر کادر لاگ ایجاد نمی‌کند.
- تاریخچه Undo پس از به‌روزرسانی پاک می‌شود؛ ذخیره خام Logcat تغییری نکرده است.

اعتبارسنجی: ۱۸ آزمون قبلی پاس شدند. تست مستقل WinForms، ورود/حذف ۶۰ هزار خط، سقف ۲۰۰۰ خط، محدودیت تعداد کاراکتر، حفظ ReadOnly و عدم رشد Undo را تأیید کرد. این تست بدون نمایش پنجره و بدون اتصال به Android اجرا شد؛ عدم شنیده‌شدن صدا در محیط کاربر هنوز تأیید نشده است.

برای تست: نسخه قبلی را ببندید، EXE داخل پوشه fix1 را اجرا کنید، Logcat را تا عبور از ۲۰۰۰ خط ادامه دهید، سپس Start Control را بررسی کنید. اگر صدا ادامه داشت، مشخص کنید صدا بدون کلیک/تایپ هم می‌آید یا فقط هنگام تعامل، و آیا متن خطای جدیدی در Log دیده می‌شود.
